name = "examscan"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
routes = [
  { pattern = "examscan.mathayomwatsing.com/*", zone_name = "mathayomwatsing.com" }
]

# Environment variables (values loaded from .env file)
[vars]
# These will be loaded from .env file during development

# No KV namespaces needed since we removed templates
# No D1 database needed since we're using Neon PostgreSQL
